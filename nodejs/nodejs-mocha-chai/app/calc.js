const add = (_first, _next)=>{
    return _first + _next;
}
const diff = (_first, _next)=>{
    return _first - _next;
}
const arr = ()=>{
    return [
        100,
        20
    ]
}


module.exports = {
    add,
    arr
}